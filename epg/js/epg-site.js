'use strict';

var epgSiteApp = angular.module('epgSiteApp', ['ui.bootstrap']);

epgSiteApp.controller('EpgSiteController', ['$scope', '$window', '$modal', function($scope, $window, $modal) {
  $scope.status = {
  };  
}]);
